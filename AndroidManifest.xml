<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="son.app.newsonline"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-sdk
        android:minSdkVersion="14"
        android:targetSdkVersion="17" />
	
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET"/>
    
    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/Theme.Newsonline">
        
        <activity android:name="son.app.newsonline.StartupPage" android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        
        <activity android:name=".ListNewspaperPage" />
        <activity android:name=".ListNews" android:label="Tin tức"/>
        <activity android:name=".AddNewspaper" android:label="Mục tin"/>
        <activity android:name=".ReadNews" ></activity>
    </application>

</manifest>